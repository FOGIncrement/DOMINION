DATABASE_URL="file:./dev.db"
JWT_SECRET="change-me-in-production"
PORT=4000
CLIENT_ORIGIN="http://localhost:5173"

# Dev/testing cheat menu (instant resources, force a tick, etc). Defaults
# off so a real deploy never accidentally exposes it — only set true locally.
ENABLE_CHEATS=false
